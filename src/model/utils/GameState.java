package model.utils;

public enum GameState {
    PLAY_STATE,
    MENU_STATE,
    PAUSE_STATE,
    GAME_OVER_STATE,
    PROFILE_STATE,
    LEVEL_SELECT_STATE,
    VICTORY_STATE,
    CHOOSE_NAME_STATE;
}
